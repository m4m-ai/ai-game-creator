import { ReactNode } from "react";
interface PropsType {
    data?: any[];
    defaultKey?: '';
    defaultRender?: ReactNode;
}
export declare function ProjectSettings(props: PropsType): JSX.Element;
export {};
